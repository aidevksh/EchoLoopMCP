{
  "name": "echoloop-mcp",
  "version": "0.1.0",
  "license": "Apache-2.0",
  "description": "MCP server that lets an agent ping you on Telegram/Discord and wait for your reply.",
  "type": "module",
  "bin": {
    "echoloop-mcp": "dist/index.js"
  },
  "files": [
    "dist",
    "scripts"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "npm run build && node --test tests/*.test.mjs",
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "proper-lockfile": "^4.1.2",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "@types/proper-lockfile": "^4.1.4",
    "typescript": "^5.9.0"
  }
}
